# Harness Audit — panther-ivy-plugin — 2026-04-28

Path: `/Users/elniak/Documents/Documents/Work/Project/Protocol-Testing-Security/PANTHER/master/.claude/worktrees/lsp-to-claude/panther/plugins/services/testers/panther_ivy/submodules/panther-ivy-plugin/plugins/panther-ivy-plugin`
Unit: `plugin`
Audit skill: `harness-audit` (v0.1)
Date: 2026-04-28
Wrapper context: invoked on `…/submodules/panther-ivy-plugin/` (a marketplace wrapper containing `.claude-plugin/marketplace.json` + `plugins/`); the actual plugin root is one level deeper.

## Scope

The plugin contains 24 skills, 12 agents, 7 commands, 8 evals, hooks, `.claude/rules/`, custom `routing-rules.json`, and `settings.json`. By scope decision (resolved during plan mode), the **11 skills marked "Deprecated under approach E orchestrator refactor (2026-04-28). Will be deleted in Phase F."** are excluded from grading. Live skills audited (13): `apt-attack-patterns`, `build-ops`, `ivy`, `ivy-syntax`, `ivy-toolkit`, `meta-self-mod-ops`, `methodology`, `propagation-patterns`, `review-ops`, `specification-patterns`, `triage-ops`, `verification-failures`, `verify-ops`. Skipped paths are listed in the Appendix.

## Summary

| Pillar | Critical | Warning | Info |
|---|---:|---:|---:|
| Context engineering | 0 | 5 | 3 |
| Harness design | 1 | 3 | 4 |
| Eval coverage | 0 | 1 | 3 |
| Prompting hygiene | 0 | 2 | 2 |
| **Total** | **1** | **11** | **12** |

### Top finding

1. **[critical] harness.dangerous_tool_creep — `agents/ivy-meta-agent.md`** — the meta agent has `Edit`, `Write`, and `Bash` (unconstrained) in its tool allowlist with `has_allowedPrompts: false`. Per the managed-agents principle, scope-limiting via tool name alone is fragile; structural constraint is what holds. The meta agent's job is to modify plugin source files (skills, agents, hooks, rules, commands), so unconstrained shell + write access amplifies blast radius.
   > Q-A4: "Auth can be bundled with a resource or held in a vault outside the sandbox." (Article A — Managed Agents)

### Notable warnings

2. **[warning] context.subagent_return_size — 5 live skills** dispatch sub-agents without an explicit return-size cap in the dispatch prompt: `build-ops`, `ivy`, `meta-self-mod-ops`, `review-ops`, `verify-ops`. Sub-agent isolation is defeated when the spawned agent fills the lead's context with an unbounded reply.
   > Q-E9: "Returns only a condensed, distilled summary of its work (often 1,000-2,000 tokens)." (Article E — Context Engineering)

3. **[warning] eval.manual_only — `evals/g{1..5}_trigger_eval.json`** — five gate trigger evals (100 cases total, 10 should-trigger + 10 should-not per gate) are documented as MANUAL execution only ("Open a fresh Claude Code session… Send the entry's prompt verbatim…"). Regressions in dispatch-hook logic ship until the next manual sweep.
   > Q-D7: "No single evaluation layer catches every issue. With multiple methods combined, failures that slip through one layer are caught by another." (Article D — Demystifying Evals)

---

## Phase 1 — Existing reviewer findings

### plugin-validator

> Source: `plugin-dev:plugin-validator` agent dispatched against `<P>` with model=opus. Verbatim summary follows.

**Summary:** Plugin parses cleanly. Manifest valid, all hook scripts resolve, MCP commands resolve, all 12 agents and 7 commands carry frontmatter, all 25 skill directories contain SKILL.md. **No critical issues.**

**Major (1):**
- `…/submodules/panther-ivy-plugin/.claude-plugin/marketplace.json:plugins[0]` — marketplace entry omits `version` while plugin manifest declares `0.10.0` (`.claude-plugin/plugin.json:5`). `/plugins install` works via local source path, but the marketplace listing shows no version. Add `"version": "0.10.0"` to the marketplace entry.

**Minor (4):**
- `.claude-plugin/plugin.json:7` — author email `elniak@github.com` is not a real GitHub-hosted address; cosmetic.
- `<P>/.claude/.backup/` — auto-loaded as project rules when plugin tree is opened as cwd; intentional per memory, but `.claude/.backup/` is shadowed by `.gitignore:5` (`**/.backup/`).
- `.backup/skills-merged-2026-04-27/` and `.backup/skills-restructure-2026-04-27/` — backup snapshots inside plugin root. Not auto-discovered (only `skills/`, `agents/`, `commands/`, `hooks/`, `output-styles/` are), so safe but bloat distribution. Per project memory, do not delete without per-file approval.
- `hooks/hooks.json:284` — `PostToolUseFailure` matcher hard-codes the tool prefix `mcp__plugin_panther-ivy-plugin_ivy-tools__`; if marketplace/plugin namespace changes, retry-ivy-mcp silently stops firing.

**Component summary:** all components valid, two MCP servers (`ivy-tools`, `serena`) wired, output-styles + styles overlays wired via `hooks/scripts/style_utils.py:40-72`, 6 routing-rules workflows defined, settings env vars match userConfig keys.

### skill-reviewer

> Source: `plugin-dev:skill-reviewer` agent dispatched against 13 live SKILL.md files (deprecated 11 excluded). Verbatim summary follows.

**Cross-cutting:**
- Ops skills bodies sprawl: `verify-ops` ≈468, `build-ops` ≈355, `review-ops` ≈372, `triage-ops` ≈366, `meta-self-mod-ops` ≈224, `ivy` ≈150. Spec budget per `skill-conventions.md`: "under 200 ideally / under 500 hard". They live just under hard ceiling. Strong candidates to extract: HARD-GATE bodies, Step Tracking templates, Journal Requirements snippets duplicated near-verbatim across 4 skills — pull into `references/ops-boilerplate.md` shared blocks or per-skill `references/step-tracking.md`.
- Every ops-skill description front-loads "Operating procedure preloaded into the …-agent at spawn", wasting the 250-char triage budget on routing metadata that the convention rule explicitly bans ("Bad: wastes description space on routing metadata").

**Per-skill verdicts:**
- `ivy/SKILL.md:3` — description 414 chars (helper measured 466), opens with "You MUST use…" (second-person). Replace with single-sentence "Use when…" trigger naming the concrete user intents (verify / build / review / triage / meta) plus knowledge-Q routing. **Major** trigger fix.
- `ivy-syntax/SKILL.md:6` — uses non-canonical `paths:` field at frontmatter top-level (the `paths:` glob belongs in a `.claude/rules/` stub, not a SKILL.md). Also keeps a stale `/set-workspace <protocol>` reference at line 13 although that slash command no longer exists. **Critical** frontmatter + stale reference.
- `ivy-toolkit/SKILL.md:3` — description fine. Body lean at 35 lines, but Integration line 33 says "Loaded by: all workflow skills and agents" while the deprecated `workflow-*` skills it references are out of scope. **Minor.**
- `apt-attack-patterns/SKILL.md:5` — uses non-standard `context: fork` frontmatter key (not in `skill-conventions.md`). Body lean (33 lines). **Minor.**
- `build-ops/SKILL.md:3` — description 250+ chars and front-loads routing metadata. ~80 lines of inline DOT graph + Step Tracking + Journal Requirements should move to `references/ops-boilerplate.md`. **Major.**
- `meta-self-mod-ops/SKILL.md:3` — same description anti-pattern. The `<iron-law>` XML block (lines 18-38) duplicates content the `.claude/rules/iron-laws.md` rule already owns. **Major** description, **Minor** dedup.
- `methodology/SKILL.md:3` — strong description; lean 35-line body; clean references. **PASS.**
- `propagation-patterns/SKILL.md:3` — strong description; embedded encoding table (lines 19-27) and Add-Field/Add-Variant patterns (lines 28-60) belong in `references/`. **Major** progressive-disclosure miss.
- `review-ops/SKILL.md:3` — same routing-metadata description issue. ≈372 lines, identical sprawl pattern. **Major.**
- `specification-patterns/SKILL.md:3` — strong description; lean 76-line body; stale `/set-workspace` reference at line 11. **Minor.**
- `triage-ops/SKILL.md:3` — same description issue; ≈366 lines, plus inline shell scripts (lines 28-66, 107-114, 145-167) totalling ~80 lines belong in `scripts/`. **Major.**
- `verification-failures/SKILL.md:3` — strong description, references-only dispatcher body at 45 lines. **PASS.**
- `verify-ops/SKILL.md:3` — same description anti-pattern; ≈468 lines (largest). HARD-GATE blocks ~50 lines repeated. Phase 6/7 fix-loop content at lines 244-329 belongs in `references/fix-loop.md`. **Major** description + length.

**Deprecated skills cross-reference (single line):** the deprecated `workflow-*` and `cross-cutting-*` skills are still cross-referenced from live `triage-ops/SKILL.md:356` (cross-cutting-reflection-patterns, cross-cutting-knowledge-capture, cross-cutting-completion-gate) and `verification-failures/SKILL.md:33,40` (workflow-triage, cross-cutting-reflection-patterns) — broken pointers post-Phase F.

**Top fixes (priority):**
1. Rewrite all 5 ops-skill descriptions to drop "preloaded at spawn" routing metadata, lead with "Use when…", stay under 250 chars.
2. Remove dangling `cross-cutting-*` and `workflow-*` references from live skills.
3. Fix `ivy-syntax` frontmatter (`paths:` invalid at SKILL.md top-level) and purge `/set-workspace` references.
4. Extract shared ops boilerplate into `references/ops-boilerplate.md` — every ops skill loses ~60-80 lines.
5. Move triage-ops shell snippets to `scripts/`; move propagation-patterns encoding tables fully into `references/`.

### claude-md-improver

> Source: `general-purpose` agent wrapping the `claude-md-management:claude-md-improver` skill. Verbatim summary follows.

No `CLAUDE.md` exists at the plugin root.

**Step 3 result:** no CLAUDE.md present at `<P>/CLAUDE.md` — flag as **MISSING** (Q-E5: structured prompt sections improve clarity; CLAUDE.md is the canonical project-context section).

**Step 4 — README.md as de-facto CLAUDE.md:** The `README.md` is 14 KB (well over 10 KB), suggesting it carries project-context, commands, and architecture detail that belongs in `CLAUDE.md`. Using `README.md` as a de-facto `CLAUDE.md` is inappropriate — `README.md` is human-facing documentation; Claude Code does not auto-load it as a system prompt.

---

## Phase 2 — Four-pillar findings

### Pillar 1 — Context engineering

- **[warning] `context.subagent_return_size` — 5 live skills dispatch sub-agents without explicit return-size cap**
  Locations:
    - `skills/build-ops/SKILL.md` (`dispatches_subagents: true`, `mentions_return_size_cap: false`, body 348 lines)
    - `skills/ivy/SKILL.md` (true / false, body 144 lines — the orchestrator)
    - `skills/meta-self-mod-ops/SKILL.md` (true / false, body 217 lines)
    - `skills/review-ops/SKILL.md` (true / false, body 365 lines)
    - `skills/verify-ops/SKILL.md` (true / false, body 461 lines)
  Quote: "Returns only a condensed, distilled summary of its work (often 1,000-2,000 tokens)." — Article E
  <https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents>
  Rationale: Sub-agent dispatch promises clean-context isolation, but only if the sub-agent's reply is bounded; otherwise the lead context fills with the sub-agent's output and the isolation benefit collapses. The orchestrator (`ivy/SKILL.md`) is the most consequential — every workflow turn dispatches one or more specialist agents.
  Fix (deterministic): in each Agent dispatch prompt inside these skills, append "Return under N words" or "Return under N tokens." A reasonable default per Q-E9 is 1,000-2,000 tokens, which maps to roughly "under 800 words". Example for `ivy/SKILL.md` dispatch table prompts: append "Return verdict and rationale in under 600 words; cite paths as `path:line`."

- **[warning] `context.progressive_disclosure` — `skills/review-ops/SKILL.md`**
  Body: 365 lines. `has_references_dir: false`. `largest_section_lines: 207`.
  Quote: "Rather than pre-processing all relevant data up front, agents built with the just-in-time approach maintain lightweight identifiers (file paths, stored queries, web links, etc.) and use these references to dynamically load data." — Article E (Q-E7)
  Rationale: review-ops has no `references/` directory at all and the largest section is 207 lines, meaning the operating procedure inlines content (Step Tracking templates, journal-requirements blocks, HARD-GATE bodies) that belongs in a referenceable companion file. Skill-reviewer corroborates with concrete extraction targets.
  Fix (description-only): scaffold `skills/review-ops/references/{step-tracking.md, journal-requirements.md, hard-gate-bodies.md}` and replace inline content with `Read references/<file>.md` pointers loaded just-in-time during the review workflow.

- **[warning] `context.progressive_disclosure` — `skills/verify-ops/SKILL.md`**
  Body: 461 lines. `has_references_dir: true`. `largest_section_lines: 305`.
  Quote: same as above (Q-E7).
  Rationale: verify-ops has the references infrastructure but isn't using it for the biggest section. A 305-line section inside a 461-line SKILL.md means two thirds of the file is one wall of inline content. Skill-reviewer identifies the fix-loop content at lines 244-329 as the chunk to extract.
  Fix (description-only): extract `references/fix-loop.md` covering lines 244-329 (Phase 6/7 fix loop) and references for HARD-GATE bodies repeated across ops skills.

- **[warning] `context.no_claude_md` — plugin root lacks `CLAUDE.md`**
  Location: `<P>/CLAUDE.md` does not exist; `<P>/README.md` is 14 KB.
  Quote: "Organizing prompts into distinct sections (like `<background_information>`, `<instructions>`, `## Tool guidance`, `## Output description`, etc) using XML or Markdown delineation improves clarity." — Article E (Q-E5)
  Rationale: The plugin has substantial project-context (LSP/MCP infrastructure, NCT/NACT/NSCT methodology, workspace conventions, iron laws) but no auto-loaded `CLAUDE.md` carrying it. README.md is human-facing documentation, not auto-loaded as a Claude Code system prompt. Claude entering the plugin directory cold has to re-discover this context every session.
  Fix (description-only): extract from README.md the project-context content (architecture, methodology selection, workspace control, iron-law primer) into `<P>/CLAUDE.md`. Keep README.md as the human-facing GitHub landing page. Do NOT duplicate iron-laws content — `<P>/.claude/rules/iron-laws.md` already auto-loads on `.ivy`/`.spec` paths.

- **[warning] `context.broken_skill_pointers` — live skills reference deprecated skills**
  Locations:
    - `skills/triage-ops/SKILL.md:355-356` references `cross-cutting-reflection-patterns`, `cross-cutting-knowledge-capture`, `cross-cutting-completion-gate` (all deprecated, scheduled for Phase F deletion)
    - `skills/verification-failures/SKILL.md:33,35,40` references `workflow-triage` and `cross-cutting-reflection-patterns` (deprecated)
  Quote: Q-E1: "Context, therefore, must be treated as a finite resource with diminishing marginal returns."
  Rationale: After Phase F deletes the deprecated skills, these references become dead pointers — Claude will follow them, hit "skill not found", and either (a) silently degrade or (b) burn tokens on retry/recovery. Easier to fix now while Phase F is still pending than to discover post-deletion.
  Fix (deterministic): replace `cross-cutting-reflection-patterns` and `cross-cutting-knowledge-capture` references with the orchestrator's G3/G6 dispatch (per the deprecation note: "folded into ivy orchestrator's G6 dispatch"). Replace `cross-cutting-completion-gate` with `skills/ivy/references/completion-gate.md`. Replace `workflow-triage` with `triage-ops`.

- **[info] `context.tool_set_overlap` — adversarial-critic agent triplet has tool_jaccard 1.0**
  Locations: `agents/g-fidelity-critic.md`, `agents/g-knowledge-critic.md`, `agents/g-plan-critic.md`. All three carry `[Read, Grep, Glob]` exactly.
  Quote: Q-E3: "Tools should be self-contained, robust to error, and extremely clear with respect to their intended use."
  Rationale: Tool-set Jaccard is 1.0 across all three critics, but description Jaccard is 0.13–0.23 — the roles are semantically disjoint (plan gate vs fidelity gate vs knowledge gate), and a read-only critic genuinely needs only `Read+Grep+Glob`. Per the pillar's cross-cutting reminder, "high overlap on names alone is suspicious; pair it with description-overlap before flagging critical." Surface as info, not warning.
  Fix: none — this is a deliberate role family, well-managed.

- **[info] `context.helper_self_flag` — harness-audit helper has `plugin.json` detection bug**
  Location: `~/.claude/skills/harness-audit/scripts/audit_helpers.py` returns `has_plugin_json: false` despite the manifest existing at `<P>/.claude-plugin/plugin.json` (the modern convention).
  Quote: Q-A3: "Harnesses encode assumptions about what Claude can't do on its own…those assumptions need to be frequently questioned because they can go stale as models improve."
  Rationale: This is a finding about the *audit tool*, not the audited plugin. The skill's own caveat ("if the user is using a recent Claude model and the skill flags an old behavior workaround, this skill should also flag itself for review") applies here in spirit — the helper's plugin-discovery logic is stuck on a path convention that has moved. Recorded for transparency; the audit is otherwise sound because Phase 1 reviewers and the manual greps cover what the helper missed.
  Fix (description-only, in the harness-audit skill): teach `audit_helpers.py` to look at `<P>/.claude-plugin/plugin.json` first, then fall back to `<P>/plugin.json`.

- **[info] `context.tool_set_overlap` — `ivy-builder-agent` ↔ `ivy-verifier-agent`**
  Locations: `agents/ivy-builder-agent.md` (11 tools), `agents/ivy-verifier-agent.md` (9 tools). `tool_jaccard: 0.538`, `desc_jaccard: 0.421`.
  Quote: Q-E4: "If a human engineer can't definitively say which tool should be used in a given situation, an AI agent can't be expected to do better."
  Rationale: Moderate overlap on both axes. The two agents are sibling specialists in the same workflow (build → verify), so some shared tool inventory is expected (Read, Grep, Glob, Skill, ivy_diagnostics, ivy_workflow_state). Below the warning threshold.
  Fix: none required. If a future refactor centralizes tool allowlists per role-family, document the rationale.

### Pillar 2 — Harness design

- **[critical] `harness.dangerous_tool_creep` — `agents/ivy-meta-agent.md`**
  Tools: `Read, Grep, Glob, Edit, Write, Bash, Skill`. `has_allowedPrompts: false`. `dangerous_tools_present: [Edit, Write, Bash]`.
  Quote: Q-A4 + Q-A5: "Auth can be bundled with a resource or held in a vault outside the sandbox… The proxy takes in a token associated with the session." — Article A
  <https://www.anthropic.com/engineering/managed-agents>
  Rationale: This rule (`harness.dangerous_tool_creep`, severity critical when `Bash AND Edit AND Write` are present without `allowedPrompts`) is exactly the case the meta agent presents. The agent's job is to modify plugin source (skills, agents, hooks, rules, commands), so the high blast radius is intrinsic to its purpose — but unconstrained Bash on top of Edit+Write means a malformed dispatch can shell out to arbitrary commands as well as touch any file. Structural narrowing via `allowedPrompts` keeps Bash invocations within an enumerated list (e.g. "run plugin tests", "git status").
  Fix (description-only): add `allowedPrompts` to `agents/ivy-meta-agent.md` frontmatter. Suggested entries (review and adjust):
    - `{tool: "Bash", prompt: "run plugin tests via pytest"}`
    - `{tool: "Bash", prompt: "git status / diff inside the plugin tree"}`
    - `{tool: "Bash", prompt: "validate plugin manifest with python -m json.tool"}`
  Anything outside that list requires user approval at runtime, which is the structural protection the article calls for.

- **[warning] `harness.dangerous_tool_creep` — `agents/ivy-triage-agent.md`**
  Tools: `Read, Grep, Glob, Bash, Skill, mcp__…__ivy_status, …__ivy_diagnostics, …__ivy_workspace, …__ivy_workflow_state`. `has_allowedPrompts: false`. `dangerous_tools_present: [Bash]`.
  Quote: Q-A4 (same as above).
  Rationale: Below the critical threshold (no Edit+Write), but unconstrained Bash without `allowedPrompts` is still flagged. The triage agent runs MCP/LSP health diagnostics; reasonable scope but no structural constraint.
  Fix (description-only): add `allowedPrompts` covering "check LSP/MCP process liveness via `ps -p`", "tail plugin log files", "inspect PID/port files in `/tmp/`". Per memory rule `feedback_no_kill_process`, do not add `kill -0`.

- **[warning] `harness.no_two_phase_init` — `skills/triage-ops/SKILL.md`**
  Body: 359 lines. `looks_long_running: true`. `has_init_section: false`.
  Quote: Q-B1: "The very first agent session uses a specialized prompt that asks the model to set up the initial environment." — Article B
  <https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents>
  Rationale: triage-ops is a long-running multi-phase health-check procedure (preflight, full health-check, repair, verify) with no explicit `## Phase 0 — Initialize` / `## Setup` section to bootstrap state (active workspace, log paths, MCP server status). Without it, Claude entering the skill cold has to discover environment state mid-procedure.
  Fix (deterministic): insert a `## Phase 0 — Setup` section at the top of `skills/triage-ops/SKILL.md` body containing: (a) `ivy_workspace(action="get")` to confirm workspace, (b) `ivy_status` to read MCP server state, (c) read of `/tmp/ivy-mcp-*.port` and `/tmp/ivy-lsp-*.pid`. The downstream phases can then assume those facts are loaded.

- **[info] `harness.dangerous_tool_creep` — `agents/ivy-builder-agent.md`** (sub-threshold)
  Tools include `Edit, Write` (no Bash). `has_allowedPrompts: false`.
  Quote: Q-A4.
  Rationale: Below the warning threshold (no Bash), but the agent edits `.ivy` specs without a structural constraint. The blast radius is bounded by the workspace, but adding `allowedPrompts: [{tool: "Edit", prompt: "edit .ivy files inside the active workspace"}, {tool: "Write", prompt: "create new .ivy files inside the active workspace"}]` would make the contract explicit.
  Fix: optional hardening; not required.

- **[info] `harness.failure_as_tool_error` — `skills/ivy/SKILL.md`** (orchestrator)
  `dispatches_subagents: true`, `handles_tool_failures: false` (helper regex did not find "if X fails / on error / fall back / retry / recover from").
  Quote: Q-A2: "If the container died, the harness caught the failure as a tool-call error and passed it back to Claude." — Article A
  Rationale: The orchestrator dispatches to specialist agents (verifier / builder / reviewer / triage / meta) but the SKILL.md body does not document what happens when a sub-agent times out, returns an error, or returns a verdict the orchestrator can't parse. May be intentionally delegated to the workflow specialists, but the orchestrator is the natural place for the first-line failure-handling rule.
  Fix (description-only): add a `## Failure handling` section near the dispatch tables: "If a specialist agent returns ABSTAIN or fails, do not retry blindly. Surface the failure to the user and ask which path to take next; if the failure is in the gate-critic vote (G0/G6), record the outcome in the journal entry and proceed without the gate verdict."

- **[info] `harness.dangerous_tool_creep` — `agents/traceability-agent.md`** (sub-threshold)
  Tools include `Write, Edit, Bash(grep|rg|find|ls|cat)`. The narrowed `Bash(<pattern> *)` form IS a structural constraint via the tool argument glob, so this falls below the warning threshold even though `dangerous_tools_present` lists Write+Edit. Recorded for completeness.
  Fix: none required.

- **[info] `harness.self_evaluation` — distinguishing the *Considerations block* from auto-self-grading**
  Locations: `README.md:129`, `output-styles/README.md:7,11`, `.claude/rules/ivy-formatting.md:2,29`, `styles/tool-renderers/ivy_verdict.md:64`, `skills/verification-failures/{SKILL.md:25, references/debugging-environment.md:3}`.
  Quote: Q-C2: "Out of the box, Claude is a poor QA agent." Q-C3: "Agents evaluating their own work respond by confidently praising the work — even when…the quality is obviously mediocre." — Article C
  <https://www.anthropic.com/engineering/harness-design-long-running-apps>
  Rationale: The plugin uses "self-review" / "self-evaluation" as the *name of a structured-presentation block* (the "Considerations" pro/con framing in tool-renderers and output-styles), not as an instruction to the model to grade its own output. Pillar's cross-cutting reminder applies: not a true violation. Recorded so future audits don't false-flag this naming.
  Fix: none required. Optional rename in the rule files to "Considerations block" to reduce future false-flagging.

### Pillar 3 — Eval coverage

- **[warning] `eval.manual_only` — gate trigger evals are MANUAL execution only**
  Location: `evals/g{1..5}_trigger_eval.json` (5 files × 20 cases = 100 cases) + `evals/orchestrator_trigger_eval.json` (20 cases). Per `evals/README.md`: "Open a fresh Claude Code session in this plugin's workspace. Send the entry's `prompt` verbatim as the first user message. Observe whether the gate fires…"
  Quote: Q-D7: "No single evaluation layer catches every issue. With multiple methods combined, failures that slip through one layer are caught by another." — Article D
  <https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents>
  Rationale: Manual-only execution means regressions in the dispatch-hook logic (file-pattern matchers, workflow-active checks, methodology overlay) ship until the next manual sweep. The hook-emitted `additionalContext` directives are deterministically observable from the hook stdout — a subset of the trigger-eval surface (specifically the "did the dispatch directive appear with the right `[G{N} ...]` tag?" check) can be automated without spinning up a live Claude session.
  Fix (description-only):
    1. Extract a `tests/test_gate_dispatch_directives.py` (or `evals/automated_gate_dispatch.py`) that synthesizes each trigger-eval prompt's preconditions (open files, workflow state) and runs the relevant hook script directly, asserting the expected `additionalContext` tag is/isn't emitted. This catches hook-logic regressions without manual-session cost.
    2. Keep the live-session manual sweep for the second-layer check (does Claude actually load reflection-patterns and dispatch critics in response to the directive). Q-D7's "multiple methods combined" is the principle.

- **[info] `eval.outcome_grading` — heavy reliance on trigger/path-style evals**
  Counts: 5 gate trigger evals (path-style: did the gate fire?) + 1 orchestrator trigger eval (path-style) + 1 workflow_dispatch_eval (15 cases, mixed) + 1 gate_critic_outcome_eval (5 gates, outcome-style).
  Quote: Q-D3: "Grade what the agent produced, not the path it took, to avoid brittleness." — Article D
  Rationale: Cross-cutting reminder applies — gate firing IS the outcome being tested, so trigger evals are not strictly path-grading in the brittleness sense. But expanding outcome-style evaluation (e.g., does the critic's verdict carry valid rationale? does the SOUND/UNSOUND/ABSTAIN distribution match the eval's expected ratio across the 100-case sweep?) would strengthen the layer mix.
  Fix (description-only): consider adding a `gate_verdict_quality_eval.json` that takes 10 representative prompts and grades the dispatched critic's verdict text against a rubric (cites `path:line`, names the failed iron law, proposes a concrete fix), independent of whether the gate fired.

- **[info] `eval.case_count` — workflow_dispatch and orchestrator evals at lower band of Q-D1 target**
  Counts: workflow_dispatch_eval = 15 cases, gate_critic_outcome_eval = 5 gates (case count per gate not measured). Trigger evals are 20 each (above the lower band).
  Quote: Q-D1: "Start early with 20-50 tasks drawn from real failures, not hundreds upfront." — Article D
  Rationale: Trigger evals at 20 cases each are inside the target. workflow_dispatch_eval at 15 is just below. Acceptable starting point; growth toward 25-30 cases drawn from real workflow-routing failures (e.g., the BGP delta-gap session paused on MCP drop documented in memory `handoff-2026-04-28`) would push it solidly into Q-D1's range.
  Fix (description-only): add ~10 more cases to `workflow_dispatch_eval.json`, sourced from the past 30 days of memory `handoff-*.md` entries that describe routing or dispatch hiccups.

- **[info] `eval.per_skill_missing` — operating-procedure skills lack per-skill evals (acceptable)**
  Locations: all 13 live skills have `evals.exists: false` at the per-skill level.
  Quote: Q-D1 (same as above), and the pillar's cross-cutting reminder.
  Rationale: Operating-procedure skills (build-ops, review-ops, verify-ops, triage-ops, meta-self-mod-ops) are spawn-time skills loaded by their respective agents — not user-invocable, so trigger evals don't fit. The orchestrator (`ivy`) IS user-invocable and is covered by `evals/orchestrator_trigger_eval.json` at the plugin level. The pillar's cross-cutting reminder says: "If the skill's purpose is intrinsically subjective…downgrade `eval.exists` to info rather than warning." Applied here.
  Fix: none required.

### Pillar 4 — Prompting hygiene

- **[warning] `prompt.skill_md_trigger_quality` — `skills/ivy/SKILL.md:3` description**
  Helper signal: `frontmatter.description_length: 466` (well over 250-char triage budget). Body line 3 starts: `description: "You MUST use this on every panther-ivy-plugin session entry where the user wants to work with .ivy specs, run formal verification, build/extend protocol models, or triage MCP/LSP health…"` — second-person opening.
  Quote: Q-E2: "Find the smallest possible set of high-signal tokens that maximize the likelihood of some desired outcome." — Article E
  Rationale: Skill-creator's own convention is third-person trigger language ("Use when…") and 250-char descriptions. Second-person "You MUST use…" is both stylistically off-pattern and triggers less reliably than a "Use when…" pattern when the model decides whether a skill applies.
  Fix (deterministic): rewrite to:
    ```yaml
    description: "Use when the user works with .ivy specs, runs formal verification, builds/extends protocol models, or triages MCP/LSP health. Routes to verifier/builder/reviewer/triage/meta agents and answers knowledge questions inline from references."
    ```
    (~245 chars, third-person, names the concrete user intents.)

- **[warning] `prompt.frontmatter_non_canonical` — `skills/ivy-syntax/SKILL.md:6` `paths:` field**
  Location: `skills/ivy-syntax/SKILL.md:1-7`:
    ```yaml
    ---
    name: ivy-syntax
    description: "Use when writing or editing .ivy files. …"
    user-invocable: false
    context: fork
    paths: "**/*.ivy"
    ---
    ```
  Quote: Q-E5: "Organizing prompts into distinct sections… using XML or Markdown delineation improves clarity."
  Rationale: SKILL.md frontmatter canonical fields are `name, description, version` (and the plugin-local extensions `user-invocable, context`). The `paths:` glob is a `.claude/rules/` frontmatter field for auto-load rules — placing it on a SKILL.md is structurally confused. The plugin already has `.claude/rules/*.md` files using `paths:` correctly (per memory note `project_plugin_rules_autoload`). Inside SKILL.md the field is silently ignored, which means the author's intent (auto-load this skill on `.ivy` paths) is not actually achieved.
  Fix (deterministic): remove the `paths: "**/*.ivy"` line from `skills/ivy-syntax/SKILL.md:6`. If auto-load on `.ivy` paths is genuinely wanted, create a stub `<P>/.claude/rules/ivy-syntax-pointer.md` with `paths: "**/*.ivy"` frontmatter and a one-line body pointing to `Skill(skill="panther-ivy-plugin:ivy-syntax")`.

- **[warning] `prompt.ops_descriptions_routing_metadata` — 5 ops-skill descriptions front-load "preloaded at spawn"**
  Locations:
    - `skills/build-ops/SKILL.md:3`: "Operating procedure preloaded into the ivy-builder-agent at spawn. Use when…"
    - `skills/review-ops/SKILL.md:3`: same pattern
    - `skills/verify-ops/SKILL.md:3`: same pattern
    - `skills/triage-ops/SKILL.md:3`: same pattern
    - `skills/meta-self-mod-ops/SKILL.md:3`: same pattern
  Quote: Q-E2: "Find the smallest possible set of high-signal tokens that maximize the likelihood of some desired outcome."
  Rationale: Per skill-conventions, the description is the trigger surface and is capped at 250 chars. Front-loading routing metadata ("preloaded at spawn", "Not user-invocable directly") consumes ~60 of those chars on information that is irrelevant to the model's trigger decision — only the "Use when…" payload tells the model when to load this skill.
  Fix (deterministic): rewrite each ops-skill description to lead with "Use when…" and move the "preloaded at spawn / not user-invocable" line into a `## When loaded` section in the body. Example for `build-ops`:
    ```yaml
    description: "Use when the ivy orchestrator dispatches the builder agent for protocol model construction (NCT/NACT/NSCT). Defines the build phases, gate-critic dispatch points, and journal-entry requirements."
    ```
    (~210 chars; the `user-invocable: false` flag in frontmatter already conveys "not user-invocable".)

- **[info] `prompt.must_overuse` — `skills/ivy-toolkit` and `skills/methodology`** (heuristic false-positive)
  Helper signal: `must_per_200_lines: 13.79` for both.
  Quote: skill-creator guidance ("explain the *why* behind everything you're asking the model to do").
  Rationale: 2 MUSTs in a 29-line body normalizes to 13.79 per 200 lines, which trips the >8 warning threshold. Absolute count is tiny. The cross-cutting reminder says: "Lean toward info over warning here unless the violation is clearly mechanical." Recorded as info.
  Fix: none required.

- **[info] `prompt.xml_section_reuse` — `<iron-law>` and `<branch>` non-canonical tags**
  Location: `skills/meta-self-mod-ops/SKILL.md` (and several deprecated skills, out of scope).
  Quote: memory rule `feedback_reuse_existing_xml_tags`: "Reuse existing XML tags before coining new ones."
  Rationale: `<iron-law>` is a consistent plugin-wide convention for load-bearing constraints with rationale; it does not duplicate a canonical tag (no canonical equivalent for "iron law"). `<branch>` is more flow-control-flavored and could be markdown headers. Surface as info — `<iron-law>` is established.
  Fix (description-only): consider replacing `<branch>` with `## Branch:` markdown headers in `meta-self-mod-ops/SKILL.md`. Keep `<iron-law>` as the plugin convention.

---

## Suggested patches

The patches below are deterministic fixes for findings ≥ warning. Apply only after reviewing each one in context — the audit is heuristic.

### Patch 1 — Add return-size cap to 5 dispatching skills

For each of `build-ops`, `ivy`, `meta-self-mod-ops`, `review-ops`, `verify-ops`:

In every Agent dispatch prompt inside `SKILL.md`, append:

```
Return verdict and rationale in under 600 words; cite paths as `path:line`.
```

(Or "under 1500 tokens" if a tighter ceiling fits the workflow.)

### Patch 2 — Fix `ivy-syntax` frontmatter

```diff
--- a/skills/ivy-syntax/SKILL.md
+++ b/skills/ivy-syntax/SKILL.md
@@ -1,7 +1,6 @@
 ---
 name: ivy-syntax
 description: "Use when writing or editing .ivy files. Provides Ivy 1.7 syntax reference, module system, RFC annotation conventions, test-spec patterns, and search-before-write practice."
 user-invocable: false
 context: fork
-paths: "**/*.ivy"
 ---
```

If auto-load on `.ivy` paths is intended, create the rule stub:

```yaml
# <P>/.claude/rules/ivy-syntax-pointer.md
---
description: "Auto-load pointer for ivy-syntax skill on .ivy file edits."
paths: "**/*.ivy"
---

When editing a `.ivy` file, invoke `Skill(skill="panther-ivy-plugin:ivy-syntax")` for canonical Ivy 1.7 syntax reference and patterns.
```

### Patch 3 — Rewrite `ivy/SKILL.md` description

```diff
--- a/skills/ivy/SKILL.md
+++ b/skills/ivy/SKILL.md
@@ -1,5 +1,5 @@
 ---
 name: ivy
-description: "You MUST use this on every panther-ivy-plugin session entry where the user wants to work with .ivy specs, run formal verification, build/extend protocol models, or triage MCP/LSP health. Routes to the matching specialist agent (verifier / builder / reviewer / triage / meta) or reads its own references for knowledge questions. This orchestrator runs first; the matching workflow specialist agent is invoked through this orchestrator's routing table, never directly."
+description: "Use when the user works with .ivy specs, runs formal verification, builds/extends protocol models, or triages MCP/LSP health. Routes to verifier/builder/reviewer/triage/meta specialist agents and answers knowledge questions inline from references; the matching workflow specialist agent is invoked through this orchestrator's routing table."
 version: "1.0.0"
 ---
```

### Patch 4 — Rewrite 5 ops-skill descriptions to drop routing metadata

Per Phase-1 skill-reviewer recommendation, rewrite each ops-skill description to lead with "Use when…" and move "preloaded at spawn / not user-invocable" into a `## When loaded` body section. The five files:
- `skills/build-ops/SKILL.md:3`
- `skills/review-ops/SKILL.md:3`
- `skills/verify-ops/SKILL.md:3`
- `skills/triage-ops/SKILL.md:3`
- `skills/meta-self-mod-ops/SKILL.md:3`

(See full per-skill rewrites in Phase-2 finding `prompt.ops_descriptions_routing_metadata`.)

### Patch 5 — Add `allowedPrompts` to `ivy-meta-agent` and `ivy-triage-agent`

`agents/ivy-meta-agent.md`:

```yaml
allowedPrompts:
  - tool: Bash
    prompt: run plugin tests via pytest
  - tool: Bash
    prompt: git status / diff inside the plugin tree
  - tool: Bash
    prompt: validate plugin manifest with python -m json.tool
```

`agents/ivy-triage-agent.md`:

```yaml
allowedPrompts:
  - tool: Bash
    prompt: check LSP/MCP process liveness via ps -p
  - tool: Bash
    prompt: tail plugin log files in /tmp/
  - tool: Bash
    prompt: inspect PID/port files in /tmp/ivy-{lsp,mcp}-*
```

(Refine the exact prompts during review — these are starting points.)

### Patch 6 — Insert `Phase 0 — Setup` into `triage-ops`

Insert at the top of the body (after frontmatter and the type/intro paragraph):

```markdown
## Phase 0 — Setup

Before any diagnostic step, load environment state:

1. `ivy_workspace(action="get")` to confirm the active workspace.
2. `ivy_status` to read MCP server liveness and version.
3. Read `/tmp/ivy-mcp-*.port` and `/tmp/ivy-lsp-*.pid` to confirm the sidecar/LSP process state.

Phases 1–5 below assume these facts are loaded. If any of the above fails, route to the repair sub-procedure (Phase 4) before continuing.
```

### Patch 7 — Remove broken pointers to deprecated skills

`skills/triage-ops/SKILL.md:355-356`:

```diff
- **Knowledge skills loaded:** `cross-cutting-reflection-patterns` (SB Phase 2, RG Phase 3), `cross-cutting-knowledge-capture` (KG Phase 3), `cross-cutting-completion-gate` (terminal verification).
+ **Knowledge skills loaded:** the orchestrator's G3 (reflection) and G6 (knowledge-capture) dispatch handle these phases; for terminal verification see `skills/ivy/references/completion-gate.md`.
```

`skills/verification-failures/SKILL.md:33,35,40`: replace `workflow-triage` with `triage-ops` and replace `cross-cutting-reflection-patterns` with the orchestrator's G3 dispatch (or with `verification-failures/references/debugging-methodology.md` where the reflection step is local).

### Patch 8 — Extract `CLAUDE.md` from README

Create `<P>/CLAUDE.md` carrying the project-context content from README.md (architecture, methodology selection, workspace control, iron-law primer pointer). Keep README.md as the human-facing landing page. Do NOT duplicate iron-law content — `<P>/.claude/rules/iron-laws.md` already auto-loads on `.ivy`/`.spec` paths.

### Patch 9 — Automate the deterministic subset of gate trigger evals

Add `tests/test_gate_dispatch_directives.py` that synthesizes each `evals/g{1..5}_trigger_eval.json` entry's preconditions, runs the relevant hook script directly, and asserts the expected `additionalContext` tag is or isn't emitted. Keep the existing manual sweep for the live-session second-layer check.

### Patch 10 — `marketplace.json` version field

Add `"version": "0.10.0"` to the marketplace wrapper's `plugins[0]` entry at `…/submodules/panther-ivy-plugin/.claude-plugin/marketplace.json`.

---

## Article-by-article traceability

Each finding above carries a quote ID like `Q-E4`. The full quote table is in `~/.claude/skills/harness-audit/references/article-quotes.md`. URLs:

- Article A — Managed Agents: <https://www.anthropic.com/engineering/managed-agents>
- Article B — Effective Harnesses for Long-Running Agents: <https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents>
- Article C — Harness Design for Long-Running Apps: <https://www.anthropic.com/engineering/harness-design-long-running-apps>
- Article D — Demystifying Evals for AI Agents: <https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents>
- Article E — Effective Context Engineering for AI Agents: <https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents>

Quote IDs cited in this report:

- Q-A2 (failure as tool error) — Pillar 2 info finding on `ivy/SKILL.md`
- Q-A3 (stale assumptions) — helper self-flag, broken-pointer cleanup
- Q-A4, Q-A5 (credential/structural isolation) — Pillar 2 critical + warning on agent tool creep
- Q-B1 (two-phase init) — Pillar 2 warning on `triage-ops`
- Q-C2, Q-C3 (Claude as poor QA) — Pillar 2 info on Considerations-block naming
- Q-D1 (eval starting size) — Pillar 3 info on case counts
- Q-D3 (outcome over path) — Pillar 3 info on trigger-eval reliance
- Q-D7 (multiple eval layers) — Pillar 3 warning on manual-only execution
- Q-E1 (context as finite resource) — broken-pointer warning
- Q-E2 (smallest set of high-signal tokens) — Pillar 4 warnings on description rewrites
- Q-E3, Q-E4 (tools self-contained, clear) — Pillar 1 info on critic-tool overlap
- Q-E5 (structured prompt sections) — Pillar 1 warning on missing CLAUDE.md
- Q-E7 (just-in-time loading) — Pillar 1 warnings on `review-ops`/`verify-ops` progressive disclosure
- Q-E9 (sub-agent return size) — Pillar 1 warning on 5 dispatching skills

---

## Appendix — Excluded skills

Skipped from the audit per the scope decision (deprecated under approach E orchestrator refactor 2026-04-28, scheduled for Phase F deletion):

1. `skills/cross-cutting-completion-gate/`
2. `skills/cross-cutting-knowledge-capture/`
3. `skills/cross-cutting-parallel-dispatch/`
4. `skills/cross-cutting-reflection-patterns/`
5. `skills/meta-plugin-self-mod/`
6. `skills/meta-using-panther-ivy-plugin/`
7. `skills/workflow-build/`
8. `skills/workflow-navigate/`
9. `skills/workflow-review/`
10. `skills/workflow-triage/`
11. `skills/workflow-verify/`

If Phase F is paused or rolled back, re-run `/harness-audit` with the "Audit normally" scope option.

## Appendix — Audit metadata

- Audit date: 2026-04-28
- Audit skill: `harness-audit` v0.1
- Audit unit: `plugin`
- Phase 1 reviewers (parallel dispatch): `plugin-dev:plugin-validator` (model=opus), `plugin-dev:skill-reviewer` (model=opus), `general-purpose` wrapping `claude-md-management:claude-md-improver` (model=sonnet).
- Phase 2 helper output: `/tmp/claude/harness-helper-out.json` (105 KB, 3258 lines).
- Saved at: `<P>/.harness-audit/report-2026-04-28.md`.
- Read-only audit: no plugin source files were modified during this run.
