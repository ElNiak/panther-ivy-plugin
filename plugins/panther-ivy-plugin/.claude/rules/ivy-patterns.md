---
paths: ["**/*.ivy"]
---

# Ivy 1.7 Language Patterns

The canonical Ivy 1.7 syntax reference moved to the project auto-memory on 2026-04-23:

- `~/.claude/projects/<project>/memory/reference_ivy_patterns.md`

Use the `Read` tool on that file when writing or editing `.ivy` files. `MEMORY.md`'s `## References` section lists this entry.
