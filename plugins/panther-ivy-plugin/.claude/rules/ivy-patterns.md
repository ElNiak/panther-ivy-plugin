---
paths: ["**/*.ivy"]
---

# Ivy 1.7 Language Patterns

Canonical Ivy 1.7 syntax reference lives inside the plugin so it is available on every install:

- `skills/ivy-writing-guide/references/ivy-1.7-patterns-reference.md`

Use the `Read` tool on that file when writing or editing `.ivy` files. The `ivy-writing-guide` skill loads it on demand during build and review workflows; this rule auto-loads the pointer on every `.ivy` edit.
