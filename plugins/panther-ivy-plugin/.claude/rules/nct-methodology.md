---
paths: ["**/*.ivy", "**/*.spec"]
---

# NCT/NACT/NSCT Methodology Reference

Canonical NCT/NACT/NSCT methodology reference (theory, 14-layer template, 10-step workflow, APT 6-stage attack lifecycle) lives inside the plugin so it is available on every install:

- `skills/methodology-reference/references/comprehensive-methodology-detail.md`

Use the `Read` tool on that file when designing protocol test architecture or scaffolding models. The `methodology-reference` skill loads it on demand during build and review workflows; this rule auto-loads the pointer on every `.ivy` or `.spec` edit.
