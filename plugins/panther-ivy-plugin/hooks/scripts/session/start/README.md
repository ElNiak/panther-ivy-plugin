# session/start/

Event-folder residual: SessionStart hooks that didn't fit a cross-event family.

| File | Event | Matcher | Purpose |
|---|---|---|---|
| `check-hook-paths.py` | SessionStart | (none) | Self-test that every `${CLAUDE_PLUGIN_ROOT}/hooks/scripts/...` path in `hooks.json` resolves on disk. |
