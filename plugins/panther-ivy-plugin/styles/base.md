# Base Output Style — Deprecated

> **This file is no longer used.** Shared formatting conventions moved to
> `.claude/rules/ivy-formatting.md` (auto-loaded by Claude Code).
> Dimension defaults are in `output-styles/` (per-style).
> Workflow-specific overrides are in `styles/overlays/` (per-workflow).
>
> See the "Style System" section in `CLAUDE.md` for the 3-layer architecture.
